materias = input("DIGITE UMA DISCIPLINA: ") #entrada string
for c in materias: # c é a variavel de posição numerica das letras, c é o valor de cada letra e enumerar é a posição que a letra está
    if c== 'a': #se c que é a psicao for igual a ent separar todos os as 3
        continue
    else:
        print(c)